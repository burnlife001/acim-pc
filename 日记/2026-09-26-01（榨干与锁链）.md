## <榨干与锁链>

我害怕被榨干，之前他们似乎能窥视我的银行卡存款（哪怕我在千里之外，几个月不通电话），每次感觉稍稍有点富余的时候，他们就整出各种幺蛾子，比如生病，各种不舒服。
在他们的世界里`生病和不舒服`是正常的，无论你如何解释，他们都不会有丝毫怀疑，这就是他们要捍卫的东西
哪怕我现在以一个一无所有的，找不到工作的，欠他们钱的儿子的面貌出现在家里，这个锁链依然存在

---

## 1.锁链形态
```python
while true:
    if 给:   被榨干 → 恨
    if 不给: 不孝 → 罪咎
```

## 2. 修正锁链：圣灵事件循环

```python
# 核心改动：不处理事件，只移交解释权，本地不存罪咎状态。
identity = "上主之子"           # 常量，非"儿子"变量
advisor = HolySpirit()         # 圣灵是唯一顾问
while true:
    signal.type = LOVE || CALL_FOR_LOVE     # 1. 统一重标：T-12.I：恐惧是爱的求助
    meaning = advisor.interpret(signal)     # 2. 移交解释权
    action = advisor.next_step()            # 给或不给
    if peace_check(action) == False:        # 不安 = advisor 被切换成了 ego
        advisor = HolySpirit()              # 重连，无重试上限，不算失败
```

**健壮性设计说明：**

* **去分支化**：旧代码的死因是两个分支都通向罪咎。新代码删掉 `给/不给` 判断——行为成为输出，不再是输入。
* **无状态**：不维护"我欠多少／我付了多少"账本。账本就是罪咎固件的持久化存储，删表即卸载。
* **故障语义反转**：失手（又内疚了、又吵了）不抛异常、不记日志、不自罚——只是重连顾问（W-95.8：错误有待修正，如此而已）。
* **单一度量**：`peace_check()` 是唯一的健康端点。平安=系统正常；不平安=该重连。
```
